{
  "name": "cooking-temperature",
  "version": "1.0.0",
  "description": "Safe cooking temperature for food",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "alexa-sdk": "^1.0.0"
  }
}
